function preload() {
  // Do any preloading you need to, like loadImage
}

function setup() {
  // Set up the area where you'll be executing your code.
  createCanvas(800, 680); // These built-in variables let you make the canvas exactly the same size as the user's window.
}

function draw() {
  // p5 Will run these instructions continuously

}
